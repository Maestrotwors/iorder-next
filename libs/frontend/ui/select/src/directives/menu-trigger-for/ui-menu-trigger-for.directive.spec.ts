import { UiMenuTriggerForDirective } from './ui-menu-trigger-for.directive';

describe('UiMenuTriggerForDirective', () => {
  it('should create an instance', () => {
    const directive = new UiMenuTriggerForDirective();
    expect(directive).toBeTruthy();
  });
});
